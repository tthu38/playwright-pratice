## Cookie  = mảnh dữ liệu nhỏ browser lưu lại
          → Ví dụ: token, user_id, preferences
## Session = phiên làm việc giữa browser và server
          → Login → server tạo session
          → Logout → session bị xóa